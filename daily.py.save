import discord
from discord.ext import commands, tasks
from discord.ext.commands import has_permissions, MissingPermissions
import discord.abc
import sys, traceback
import bot_config
import psutil
import os
import random
import re

async def sendDaily(message, numDays):
	count = 1
	await message.channel.send("Attempting to send messages every minute")
	self.dailyMsg(message).start()


@tasks.loop(minutes=1)
async def dailyMsg(self, message):
	string = "Hey all, AccountaBuddy here. I'm here to check in on your chat."
	await message.channel.send(string)
